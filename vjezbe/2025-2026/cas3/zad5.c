#include <stdio.h>

int main() {

    int x = 5;
    int y = 9;

    // x = x + 2;
    // x += 2;

    // x = x + y;
    // x += y;

    // x = x - 3;
    // x -= 3;

    // x = x - y;
    // x -= y;

    // x = x * 3;
    // x *= 3;

    // x = x / 2;
    // x /= 2;

    // x = x % 2;
    // x %= 2;

    // x = x + 1;
    // x += 1;
    // x++;
    // ++x;

    // printf("x = %d\n", x);

    // y = x++; //prvo se dodijeli y = x. Zatim se x uveca za 1.
    // printf("x = %d\n", x);
    // printf("y = %d\n", y);
    
    // y = ++x; //Prvo se x uveca za 1. Pa se uvecana vrijednost dodijeli y;
    // printf("x = %d\n", x);
    // printf("y = %d\n", y);

    // printf("%d\n", x++); //stampa 5 i x postaje 6
    // printf("%d\n", ++x); //x posta 7 i stampa se 7

    // int z = y += x + 2;

    // printf("x = %d\n", x);
    // printf("y = %d\n", y);
    // printf("z = %d\n", z);

    printf("%d\n", x--);
    printf("%d\n", --x);

    return 0;
}